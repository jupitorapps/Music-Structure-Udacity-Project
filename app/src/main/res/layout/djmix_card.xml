<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    style="@style/cardStyle">
    <RelativeLayout
        android:id="@+id/djmixCard"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/djmixCardImage"
            style="@style/cardImageViewStyle"
            android:contentDescription="@string/djMixRingtone"
            android:src="@drawable/djmix"/>

        <TextView
            style="@style/cardTextViewStyle"
            android:id="@+id/djmixText"
            android:text="@string/djMixRingtone" />
    </RelativeLayout>


</android.support.v7.widget.CardView>
